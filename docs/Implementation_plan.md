Implementation Plan — cpp-text-analyzer
1. Краткое описание задачи
Разработать консольное приложение на языке C++ для анализа частоты слов в текстовых данных формата JSON.
Программа выполняет:

Чтение JSON-файлов с текстом и стоп-словами

Генерацию тестовых JSON-файлов для тестирования

Разбиение текста на слова с фильтрацией стоп-слов

Подсчет частоты встречаемости слов

Вывод топ-N самых частых слов

Бенчмарк производительности на множестве файлов

2. Цель и ожидаемый результат
Цель работы — освоить работу с JSON в C++, обработку UTF-8 текста, алгоритмы подсчета частоты и измерение производительности.

Ожидаемый результат:
Консольное приложение (.exe), которое:

 Принимает JSON-файлы или генерирует их (text_0.json, text_1.json...)

 Корректно обрабатывает русский текст (UTF-8 кириллица)

 Фильтрует стоп-слова ("и", "в", "у")

 Выводит статистику: общее количество слов, уникальных слов, топ-5

 Измеряет время обработки для бенчмарка

 Работает стабильно с 1-1000+ файлов

3. Этапы и задачи реализации
Анализ формата входных данных
 Структура JSON: {"text": "строка текста", "stopwords": ["и", "в", "у"]}

 Обязательные поля: text (string)

 Опциональные поля: stopwords (array строк)

Проектирование структуры программы
```text
WordFrequencyAnalyzer/
├── src/main.cpp          # Главная логика + меню
├── data/                 # Тестовые JSON файлы
├── docs/                 # Документация
└── README.md            # Инструкция
```
Реализация генерации и проверки файлов
generateJsonFiles() — создание text_N.json с 4 типами текстов

readJsonFile() — чтение с обработкой ошибок (пустой файл, неверный JSON)

validateTextJson() — проверка структуры JSON

Парсинг и преобразование данных
 nlohmann/json — чтение JSON в C++ объекты

 splitWords() — разбиение текста на слова через stringstream

 UTF-8 совместимость с Windows консолью

Валидация данных
 Проверка наличия поля text

 Проверка типов данных (string для text, array строк для stopwords)

 Обработка ошибок парсинга JSON

Вычисления и алгоритмы
 Подсчет частоты через std::map<string, int>

 Сортировка топ-N слов через std::vector<pair> + sort()

 Бенчмарк времени с std::chrono::high_resolution_clock

Вывод результатов пользователю
```text
📄 Файл #0 (text_0.json):
Всего слов: 31, уникальных: 2
Топ-5:
  1) 'Слово' - 12 раз
  2) 'было' - 19 раз
----------------------------------------
 Общее время (100 файлов): 245 мс
```
Тестирование и исправление ошибок
 Тестированиправлене на 1, 10, 100 файлах

 Обработка UTF-8 артефактов (�)

 Исие cin.ignore() для корректного ввода

 Отладка логики splitWords()

4. План-график выполнения 
Этап	                    Срок (дни)
1. Анализ формата данных	   1–2	
2. Проектирование структуры	  3
3. Генерация/проверка файлов 4–5
4. Парсинг данных	           6–7	
5. Обработка текста	         8–10	
6. Вычисления частоты	       11–12	
7. Вывод результатов	        13	
8. UTF-8 исправления	       14–16
9. Тестирование	             17–18
10. Финальная сборка	        19
Общее время разработки: 19 дней

5. Ресурсы
Язык программирования: C++17
Среда разработки: Visual Studio Code + MSVC
Библиотеки:

nlohmann/json — парсинг JSON

std::chrono — бенчмарк

Windows.h — UTF-8 консоль

Входные данные:

```json
{
  "text": "В начале было Слово и Слово было у Бога.",
  "stopwords": ["и", "в", "у"]
}
```
Документация:

nlohmann/json

C++ UTF-8 Windows Console

Методические указания лабораторной работы
